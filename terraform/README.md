# Terraform Configuration

This directory contains Terraform configuration files for managing the global infrastructure:

- Google Cloud Project setup
- API enablement
- Firestore database configuration
- Cloud Run service deployment
- IAM and permissions

## Structure

Add your Terraform `.tf` files here to manage infrastructure as code.

## Usage

```bash
terraform init
terraform plan
terraform apply
```
